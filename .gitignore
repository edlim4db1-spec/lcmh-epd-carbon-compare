# dependencies / build
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# local node toolchain (kept outside repo anyway)
../.tools/

# OS
.DS_Store

# extraction working files (candidates/corrections are intermediate; /data is the deliverable)
extract_work/
epds_raw/

# NOTE: /data (final JSON) and /epds (source PDFs for provenance) ARE committed.
